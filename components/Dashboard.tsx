import React from 'react';

// This file was empty, which can cause build errors.
// It is now a valid, empty component to prevent the app from crashing on load.
// This component is not used anywhere in the application.
const Dashboard: React.FC = () => {
  return null;
};

export default Dashboard;
